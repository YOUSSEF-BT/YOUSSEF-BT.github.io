name: Deploy portfolio to GitHub Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Rebuild PFE cover image
        run: |
          mkdir -p public/assets/images/projects/accident-detection
          cat build-assets/pfe-cover/part*.b64 | tr -d '\n' | base64 --decode > public/assets/images/projects/accident-detection/main-cover-photo.webp

      - name: Build portfolio
        run: npm run build

      - name: Use photo only as PFE project cover
        run: |
          find dist/assets -type f -name '*.js' -exec sed -i 's#assets/images/projects/accident-detection/main-cover-vector\.svg#assets/images/projects/accident-detection/main-cover-photo.webp#g' {} +

      - name: Publish complete build to gh-pages branch
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
          publish_branch: gh-pages
          force_orphan: true
          enable_jekyll: false

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: ./dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
