name: Deploy complete portfolio to gh-pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: portfolio-gh-pages-v2
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 12

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci --no-audit --no-fund --prefer-offline
        env:
          npm_config_fetch_retries: 2
          npm_config_fetch_retry_maxtimeout: 30000
          npm_config_fetch_timeout: 60000

      - name: Build portfolio
        run: npm run build
        env:
          # Public backend URL only — never put GEMINI_API_KEY in the frontend.
          # Leave the repository variable unset until the deployed assistant has
          # passed /health and the online evaluation workflow.
          VITE_ASK_YOUSSEF_API_URL: ${{ vars.ASK_YOUSSEF_API_URL }}

      - name: Publish complete dist directory
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: gh-pages
          publish_dir: ./dist
          force_orphan: true
          enable_jekyll: false
